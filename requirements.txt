fastapi>=0.110.0
uvicorn[standard]>=0.27.0  # Includes websockets and wsproto for WebSocket support
sqlalchemy>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.0
cryptography>=41.0.0
python-multipart
aiofiles
httpx
requests
tqdm
python-dotenv
loguru
psutil
humanize
pydub

# TTS Engines
TTS>=0.22.0
suno-bark
# tortoise-tts temporarily disabled due to transformers conflict with TTS
# tortoise-tts

# Pinned dependencies to resolve conflicts
bangla<0.0.5  # Fixed Python 3.10 syntax error on Python 3.9
transformers>=4.33.0,<4.40.0  # Compatible with TTS>=0.22.0
bcrypt>=3.2.0,<4.0.0  # Resolves passlib ValueError on password length detection and bcrypt 4.x compatibility issues


# STT Engines
faster-whisper>=1.0.0
openai-whisper

# Music Generation (using transformers instead of audiocraft to avoid PyAV dependency)

# ML Core
torch>=2.1.0
torchaudio>=2.1.0
